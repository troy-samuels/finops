[project]
id = "finops-tracker"

[api]
port = 54321
schemas = ["public"]

[db]
port = 54322

[studio]
port = 54323

# Edge Functions — both use custom auth, not Supabase JWT
[functions.track-event]
verify_jwt = false

[functions.sync-pricing]
verify_jwt = false
